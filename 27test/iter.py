# 迭代器和可迭代对象
# 可迭代对象，凡是可以跟在for循环后面的，都是可迭代对象
# 比如字符串，列表，元组，字典，集合
# 可迭代对象的唯一的接口'__iter__'
# 对应的调用方法iter()

# 迭代器的接口'__iter__','__next__'

# 迭代器的意义
# 统一的通过next()方法获取数据，可以屏蔽底层不同数据的读取方式，简化编程
# 容器类的数据结构只关心数据的静态存储，每一层迭代都需要额外的迭代器对象专门记录迭代过程中的状态信息

# 迭代器为了让数据结构能够快捷的遍历而定义的辅助对象